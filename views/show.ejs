<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        #container{
            padding: 100px;
            padding-top: 20px;
            /* margin: auto; */
            text-align: center;
        }
        .view{
            text-decoration: none;
            padding: 10px;
            background-color: black;
            color: rgb(252, 252, 252);
            border: 3px;
            /* margin-bottom: 10px; */
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div id = "container">
        
        <h1>Title :<span style="font-weight: lighter;"><%= account.title %></span> </h1>
        <h1>Username :<span style="font-weight: lighter;"> <%= account.username %></span></h1>
        <h1>Email  :<span style="font-weight: lighter;"><%= account.email %> </span></h1>
        <h1>Password : <span style="font-weight: lighter;"><%= password %></span></h1>
        <form action="/accounts/<%= account._id %>?_method=DELETE" method="POST" >
            <button type="submit" class="view" >Delete</button><br><br>
        </form>
        <a href = "/accounts/<%= account._id %>/edit" class="view" >Edit</a>
    </div>
</body>
</html>